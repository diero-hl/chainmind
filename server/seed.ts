// No seeding needed for Moltbook Agent
// Agent is registered by user through the UI

export async function seedDatabase() {
  console.log("Moltbook Agent initialized - register your agent through the UI");
}
